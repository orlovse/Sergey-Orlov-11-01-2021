export const darkThemeSelector = (state) => state.theme.isDark;
export const currentLocationSelector = (state) =>
  state.locations.currentLocation;
export const currentWeather = (state) => state.currentWeather.entities;
